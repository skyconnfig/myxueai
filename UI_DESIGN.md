# XueAI Video Factory — 交互原型 UI 设计文档

> 文档用途：为产品经理 / 设计师提供可直接制作可交互原型图（Figma / Pixso / 墨刀）的完整 UI 规范。
> 版本：2026-08-13

---

## 1. 设计系统 Design System

### 1.1 品牌基础

| 项目 | 值 |
|------|-----|
| 品牌名 | XueAI Video Factory |
| 定位 | AI 视频生产操作系统 |
| 核心隐喻 | Agent + Skill + Scene Engine + Remotion |
| 主色调 | 科技蓝 `#3B82F6` / 冷灰 `#0F172A` |
| 辅助色 | 紫色 `#8B5CF6`、青绿 `#10B981`、橙色 `#F59E0B`、红色 `#EF4444` |
| 背景色 | 深色 `#0B0D10` / 卡片 `#151A23` / 玻璃 `rgba(21,26,35,0.72)` |
| 文字色 | 主白 `#FFFFFF` / 次级 `#94A3B8` / 禁用 `#64748B` |
| 圆角 | 卡片 `12–18px` / 按钮 `10px` / 输入框 `10px` / 胶囊 `999px` |
| 字体 | 主字体 Inter / 系统字体；中文 PingFang SC、Microsoft YaHei；等宽 `ui-monospace` |
| 阴影 | 卡片 `0 24px 64px rgba(0,0,0,0.35)` / 辉光 `0 0 24px rgba(59,130,246,0.12)` |
| 动效 | 入场 `cubic-bezier(0.22, 1, 0.36, 1)` / 悬停 150ms / 骨架屏 pulse |

### 1.2 通用组件

#### 玻璃卡片 `.glass-panel`
- 背景：`rgba(21, 26, 35, 0.72)` + `backdrop-filter: blur(16px)`
- 边框：`1px solid rgba(255,255,255,0.08)`
- 圆角：`16px`
- 内边距：`16px` 或 `24px`
- 悬停：边框变为 `rgba(59,130,246,0.25)`，带轻微 translateY(-2px)

#### 按钮规范

| 按钮类型 | 样式 | 用途 |
|----------|------|------|
| 主按钮 | 渐变 `linear-gradient(135deg, #3B82F6, #8B5CF6)` | 主要操作：创建、生成、保存 |
| 次按钮 | 背景 `#1E293B`、边框 `#334155` | 次要操作：取消、返回 |
| 幽灵按钮 | 透明背景 + 边框 | 筛选、分页、选择 |
| 危险按钮 | 背景 `rgba(239,68,68,0.15)`、边框红色 | 删除、停止 |
| 图标按钮 | 36×36，圆角 10px | 头部工具栏 |

#### 输入框
- 高度 `44px`，背景 `#0B0D10`，边框 `#334155`
- focus：`border-color: #3B82F6`，外发光 `0 0 0 3px rgba(59,130,246,0.18)`
- placeholder 颜色 `#6B7280`

#### 标签 / 徽章
- 等宽字体 `10px`，胶囊 `px-2 py-0.5`，背景 `rgba(59,130,246,0.12)`，边框 `rgba(59,130,246,0.25)`
- Skill 类型标签：Bundle（紫）、Style（蓝）、Caption（青）、Audio（橙）、Camera（灰）

---

## 2. 信息架构 IA

```
Public
├── /login       登录
├── /register    注册

AppLayout (登录后)
├── 创作
│   ├── AI Studio         /create
│   ├── 项目中心          /dashboard
│   ├── AI 方案           /projects/:id/plan
│   ├── AI 导演           /projects/:id/director
│   └── 生产进度          /projects/:id/production
├── 内容资产
│   ├── 素材库            /assets
│   ├── 模板库            /templates
│   └── Skill 市场        /skills
├── 增长
│   ├── 分发中心          /dashboard
│   └── 生产统计          /dashboard
└── 系统
    └── 设置              /settings
```

---

## 3. 页面详细设计

### 3.1 登录页 /login

#### 布局
- 全屏深色背景，左侧品牌视觉区（桌面端占约 55%），右侧表单区（占约 45%）
- 移动端：仅显示表单，顶部小尺寸插画
- 背景装饰：两个模糊光球（左上蓝、右下紫）+ 48px 网格线渐变遮罩

#### 左侧品牌区
- 顶部 Logo：圆角方块内白色/渐变 `X`，右侧 `XueAI` + `Video Factory`
- 插画：Storyset 风格 SVG（视频创作 / 导演主题），最大宽度 420px
- 标题：「AI 视频生产操作系统」
- 副标题：「输入一个想法，Agent 自动完成脚本、分镜、素材与渲染」
- 流程标签：
  1. 输入账号
  2. 验证身份
  3. 进入工作台
- 底部三个胶囊：自动分镜、AI 脚本、一键渲染

#### 右侧表单区
- 卡片标题：「登录账户」
- 卡片描述：「继续你的视频生产任务」
- 表单字段：
  - 邮箱（email，placeholder: you@example.com）
  - 密码（password，placeholder: 至少 6 位）
- 主按钮：「登录」+ 箭头图标
- 次按钮：「使用演示账号」+ 小号邮箱 `demo@xueai.local`
- 底部切换：还没有账户？注册
- 页脚插画署名链接

#### 交互状态
- 输入错误：边框变红，下方显示 `text-xs text-red-400` 错误文案
- 加载：按钮显示 spinner，文案变为「登录中...」
- 登录成功：toast 成功，1s 后跳转 `/dashboard`

#### 原型注释
- 插画可用占位图：标明为 Storyset 插画占位
- 演示账号按钮建议作为独立次级入口，便于用户快速体验

---

### 3.2 注册页 /register

#### 布局
- 与登录页一致，左侧插画文案切换为注册主题
- 流程标签：
  1. 填写信息
  2. 创建账户
  3. 开始创作

#### 右侧表单区
- 卡片标题：「创建账户」
- 卡片描述：「免费注册，立即获得 AI 点数」
- 表单字段：
  - 昵称（可选，placeholder: 如何称呼你）
  - 邮箱（必填）
  - 密码（必填，至少 6 位）
  - 确认密码（必填）
- 协议文案：注册即表示你同意使用 XueAI 进行 AI 辅助视频创作，并获得初始 AI 点数。
- 主按钮：「创建账户」+ 箭头图标
- 底部切换：已有账户？登录

#### 交互状态
- 密码不一致：聚焦确认密码，显示「两次输入的密码不一致」
- 注册成功：toast 成功，自动跳转 `/dashboard`

---

### 3.3 应用布局 AppLayout

#### 顶部栏 AppHeader（高度 56px）
- 左侧：
  - Logo：圆角方块内蓝色 `X` + `XueAI` + `Video Factory` 标签
  - 分隔线
  - 项目选择器：下拉按钮，显示当前项目名 / 选择项目，右侧 ChevronDown
  - 保存状态徽章：绿点 + 「已保存」 / 黄点 + 「同步中...」
- 中部（仅在 Studio 页面显示）：
  - 比例切换：9:16 / 16:9 / 1:1，图标+文字，当前项高亮
- 右侧：
  - AI 点数：`Coins` 图标 + 数值
  - 任务中心：`ListTodo` 图标 + Badge，点击展开任务面板
  - 新建按钮：`Sparkles` + 新建
  - 开始渲染：`Play` 图标 + 开始渲染（主按钮样式）
  - 头像：圆形，取用户名前两位大写，点击跳转 /login（退出）

#### 任务面板浮层（点击任务图标展开）
- 尺寸：宽 320px，圆角 16px，玻璃卡片
- 标题：AI 任务中心 + 右上角「+ 新建任务」
- 列表项：项目名称、任务类型、状态、进度
- 操作：停止（RUNNING）、删除（非 RUNNING）
- 底部统计：正在生成 N 个 / 队列等待 N 个

#### 侧边栏 AppSidebar（宽度 224px）
- 顶部：Workspace 标签
- 导航分组（创作 / 内容资产 / 增长 / 系统）
- 导航项：图标 + 文字，当前项蓝色高亮
- 底部资源卡片：
  - 存储进度条（12%）
  - AI 点数余额

---

### 3.4 项目中心 /dashboard

#### 页面布局
- 无 Studio 上下栏，主区域可滚动
- 顶部 Hero 区：
  - 标题：项目中心
  - 描述：管理你所有的 AI 视频生产任务
  - 主按钮：「新建视频」+ 次按钮「导入项目」
- 统计卡片行：
  - 总项目数
  - 本周生成
  - 渲染完成
  - 剩余 AI 点数

#### 项目卡片网格
- 3 列（桌面）/ 2 列（平板）/ 1 列（移动端）
- 每个卡片：
  - 缩略图（16:9 或 9:16 根据项目比例）
  - 状态标签：草稿 / 渲染中 / 已完成
  - 项目名
  - 类型 + 时长 + 镜头数
  - 底部操作：编辑、渲染、删除（hover 显示）
- 空态：「暂无项目，点击新建视频开始创作」

---

### 3.5 AI Studio /create

#### 页面布局
- 居中窄布局（max-w-2xl），全页可滚动
- 顶部：
  - 徽章：AI 导演中心
  - 标题：输入一个想法，AI 完成剩下的事
  - 描述：导演 Brief · 故事弧 · 电影分镜 · 素材 · 渲染
  - 已选 Skill 徽章行（如果有）：列出已选 Skill ID + 管理入口

#### 输入表单
- 主题输入框：大字号，placeholder：「例如：帮我做一个 30 秒 AI 办公软件产品介绍视频」
- 目标受众：下拉 / 输入，如「企业运营管理者」
- 营销目标：下拉，如 conversion / awareness / education
- 视频比例：三图标选择（9:16 / 16:9 / 1:1）
- 时长：滑块 + 数字输入（5–300 秒，默认 30）
- 风格：下拉选择（如科技干货 / apple_saas_commercial）
- 主按钮：「AI 创建视频」+ Sparkles 图标

#### 模板推荐区（可选展开）
- 横向滚动模板卡片，点击填充表单

#### 已选 Skill 交互
- 在标题下方显示已选 Skill chips（蓝紫色）
- 点击「管理」跳转 `/skills`
- 从 `/skills` 返回后自动读取 query 中的 `skills` 并填充

#### 提交反馈
- 加载：按钮 disabled，显示「AI 正在理解需求...」
- 成功：跳转 `/projects/:id/plan`
- 失败：toast 提示，保留表单状态

---

### 3.6 AI 方案 /projects/:id/plan（VideoPlan Studio）

#### 整体布局
- 顶部：CreationPipeline 步骤条（inspire / script / storyboard / material / edit / publish）
- 中部：三栏布局
  - 左栏（260px）：AI 创建面板 + AI 导演面板
  - 中栏（flex）：视频预览区
  - 右栏（280px）：分镜时间线 + 场景编辑器

#### 步骤条 CreationPipeline
- 6 步：灵感 → 脚本 → 故事板 → 素材 → 编辑 → 发布
- 当前步高亮，已完成步显示 ✓，未进行步灰色
- 可点击切换（但需有数据才能进入后续步骤）

#### 左栏：AI 创建面板
- 标题：AI 重新生成
- 输入：主题 / 风格 / 时长
- 按钮：生成脚本
- 加载状态：显示 spinner + 进度文字
- 生成的脚本来源标签：DeepSeek AI / 智能预设

#### 左栏：AI 导演面板
- 导演 Brief 展示：
  - 视频风格
  - 情绪
  - 受众
  - 目标
  - 故事弧（5 段 beats）
- 按钮：编辑 Brief、应用风格

#### 中栏：视频预览 VideoPreviewStudio
- 顶部：播放控制、时间轴、静音/音量、字幕开关
- 主区域：视频预览画布（按项目比例自适应）
- 空态：「点击生成脚本开始预览」
- 底部：当前场景信息、重新生成画面按钮

#### 右栏：StoryTimeline
- 垂直时间线，每个场景为一个节点
- 节点内容：
  - 场景编号
  - 缩略图
  - 故事 beat（hook / problem / solution...）
  - 时长
  - 镜头类型 + 运镜
- 选中态：蓝色边框
- 操作：添加场景、删除场景、AI 优化单镜

#### 场景编辑器（选中场景后底部或右侧展开）
- 表单字段：标题、描述、画外音、时长、镜头、运镜、情绪、灯光、转场
- 按钮：保存、重新生成画面、重新配音、删除

---

### 3.7 生产进度 /projects/:id/production

#### 页面布局
- 居中卡片，显示当前渲染流水线
- 步骤列表：
  1. DIRECTOR 导演
  2. SCRIPT 脚本
  3. STORYBOARD 故事板
  4. ASSET 素材生成
  5. TTS 配音
  6. TIMELINE 合成
  7. RENDER 渲染
- 每步：状态图标 + 名称 + 进度条 + 耗时
- 底部：取消按钮 / 查看结果按钮
- 完成态：显示「MP4 已输出」+ 下载 / 预览按钮

---

### 3.8 素材库 /assets

#### 页面布局
- 顶部 Hero：素材库 + 上传按钮
- 搜索框 + 类型筛选（全部 / 图片 / 音频 / 视频）
- 网格卡片：
  - 预览（图片直接展示，音频/视频用图标）
  - 文件名
  - 类型标签
  - 上传时间
  - 删除按钮
- 空态：「暂无素材，点击上传」

---

### 3.9 模板库 /templates

#### 页面布局
- 顶部 Hero：
  - 徽章：模板市场 · Template Market
  - 标题：热门视频模板
  - 描述：一键套用爆款结构，AI 自动完成文案与分镜
  - 按钮：从空白创建
- 模板卡片网格：
  - 缩略图 + 标签 + 时长比例 + 名称 + 描述
  - 按钮：使用模板

---

### 3.10 Skill 市场 /skills

#### 页面布局
- 顶部 Hero：
  - 徽章：Skill OS · Agent 能力市场
  - 标题：XueAI Skill Marketplace
  - 描述：Agent + Skill + Scene Engine + Remotion — 选择专业视频制作能力
  - 按钮：上传 Skill（次）、使用已选 N（主，disabled 当未选择）

#### 智能匹配区
- 输入框：placeholder「输入：制作 AI 软件介绍视频 / 知识科普 / 品牌广告...」
- 按钮：智能匹配
- 结果：匹配的 Skill ID chips（蓝紫色等宽标签）

#### 筛选区
- 搜索框
- 分类下拉：视频风格 / 字幕 / 音频 / 视频类型

#### 标签页
- 官方 Skill
- 我的 Skill

#### Skill 卡片
- 卡片头部：
  - 类型标签（Bundle / Style / Caption / Audio / Scene / Camera / Hook）
  - 精选标识（可选）
  - 层级标签（官方 / 社区 / 用户）
- 标题
- 简介（最多 2 行）
- 标签行：最多 4 个 tag chips
- 底部操作：
  - 使用 Skill（主按钮）
  - + 选择（切换选中态）
- 选中态：蓝色边框 + 环形高亮

#### 我的 Skill 标签页
- 列出用户上传 Skill
- 每个卡片：ID、名称、简介、删除按钮、使用按钮
- 空态：上传 Skill 引导

#### 上传 Skill 弹窗
- 标题：上传自定义 Skill
- 字段：作者名（可选）、简介（可选）
- 文本域：YAML 内容，等宽字体，14 行高
- 按钮：验证并上传
- 上传成功：关闭弹窗，切换到「我的 Skill」

---

### 3.11 设置 /settings

#### 页面布局
- 左侧 Tab 导航，右侧内容区
- Tab 项：
  - 账户信息
  - API 密钥
  - 工作区偏好
  - 积分与账单
  - 通知
- 内容区：对应表单与开关

---

## 4. 关键交互流程（原型用例）

### 用例 A：注册 → 创建视频 → 生成脚本
1. 用户打开 `/register`，填写昵称/邮箱/密码，点击「创建账户」
2. 进入 `/dashboard`，点击「新建视频」
3. 进入 `/create`，输入主题，选择比例/时长/风格
4. 点击「AI 创建视频」
5. 进入 `/projects/:id/plan`，显示步骤条与加载动画
6. 生成完成后展示故事板、预览、场景列表

### 用例 B：使用 Skill Marketplace 创建产品演示视频
1. 用户点击侧边栏「Skill 市场」
2. 在搜索框输入「AI 软件介绍」
3. 点击「智能匹配」，系统匹配 `style.technology`、`caption.kinetic-tech`、`bundle.product-demo`
4. 点击 Skill 卡片上的「使用 Skill」或批量选择后点击「使用已选」
5. 跳转 `/create`，主题自动填充，已选 Skill chips 显示在顶部
6. 点击「AI 创建视频」，脚本强制使用 ProductDemoV2 + uiSteps

### 用例 C：用户上传自定义 Skill
1. 在 `/skills` 点击「上传 Skill」
2. 弹窗中粘贴 YAML，填写作者名/简介
3. 点击「验证并上传」
4. 成功后弹窗关闭，切换到「我的 Skill」查看新 Skill
5. 点击「使用此 Skill」进入创建流程

---

## 5. 响应式断点

| 断点 | 范围 | 布局调整 |
|------|------|----------|
| 移动端 | < 640px | 侧边栏隐藏为抽屉，顶部栏简化，卡片 1 列 |
| 平板 | 640–1024px | 侧边栏可收起，卡片 2 列，Studio 中栏缩小 |
| 桌面 | 1024–1440px | 完整三栏 Studio，卡片 3 列 |
| 大屏 | > 1440px | 卡片 4 列，预览区更大 |

---

## 6. 原型制作建议

### 推荐工具
- Figma（最佳组件化与交互）
- Pixso（中文协作友好）
- 墨刀（快速可交互原型）

### 必做页面清单
- [ ] 登录页
- [ ] 注册页
- [ ] 应用布局（顶部栏 + 侧边栏）
- [ ] 项目中心
- [ ] AI Studio（创建视频）
- [ ] AI 方案 Studio（三栏布局）
- [ ] 生产进度
- [ ] 素材库
- [ ] 模板库
- [ ] Skill 市场
- [ ] 设置

### 交互原型重点
1. 登录/注册页入场动画：左侧品牌元素依次淡入
2. 创建视频流程：表单 → 加载 → 跳转 Studio
3. Skill 市场：选择态、匹配动画、上传弹窗
4. Studio 页面：步骤条切换、场景时间线选中、预览播放
5. 任务面板浮层：点击展开/收起，任务状态变化

### 图标库
- 使用 `lucide` 图标（已在前端使用）
- 关键图标：
  - 登录：ArrowRight
  - 新建：Sparkles, Plus
  - 渲染：Play
  - 任务：ListTodo
  - 素材：Layers, Image, Music, Film
  - 模板：LayoutTemplate
  - Skill：Brain, Wand2, Zap
  - 设置：Settings

---

## 7. 文案规范

- 使用中文为主，专业技术术语可保留英文：Skill、Agent、AI Director、Remotion
- 按钮文案简洁：「AI 创建视频」「开始渲染」「使用 Skill」
- 状态文案清晰：「脚本生成中...」「渲染完成」「同步中...」
- 空态文案友好：「暂无项目，点击新建视频开始创作」

---

## 8. 设计原则

1. **深色优先**：主界面深色，减少视频创作时的视觉干扰
2. **信息密度**：每屏信息丰富但层级清晰，用卡片和间距分隔
3. **玻璃拟态**：适度使用毛玻璃效果，保持科技感
4. **即时反馈**：所有操作都有加载、toast、状态变化
5. **Skill 可视化**：Skill 市场在 UI 中始终可见，强化「AI Video OS」心智
6. **生产导向**：所有设计围绕「从想法到 MP4」的流水线展开

---

*文档结束。可用此文件直接在 Figma / Pixso / 墨刀中制作可交互原型。*
